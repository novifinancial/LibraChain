name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: opam install
      uses: huitseeker/opam-coq-action@master
    - name: build
      run: make clean && make

